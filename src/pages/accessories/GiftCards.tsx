import ProductGrid from '../../components/ProductGrid';

export default function GiftCards() {
  return (
    <ProductGrid
      products={[]}
      title="Gift Cards"
      description="The perfect gift for any occasion. Available in digital and physical formats."
    />
  );
}