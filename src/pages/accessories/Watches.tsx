import ProductGrid from '../../components/ProductGrid';

export default function Watches() {
  return (
    <ProductGrid
      products={[]}
      title="Watches"
      description="Sustainable and stylish timepieces for every occasion. From classic minimalist designs to modern sport watches."
    />
  );
}