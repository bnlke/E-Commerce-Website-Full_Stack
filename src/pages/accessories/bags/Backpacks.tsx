import ProductGrid from '../../../components/ProductGrid';

export default function Backpacks() {
  return (
    <ProductGrid
      products={[]}
      title="Backpacks"
      description="Sustainable backpacks for every adventure. From city commutes to trail hiking."
    />
  );
}